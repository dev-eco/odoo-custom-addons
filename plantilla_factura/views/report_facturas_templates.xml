<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--
        ============================================================================
        PLANTILLA QWEB PARA REPORTE DE FACTURAS PERSONALIZADO - ECOCAUCHO v2.3
        ============================================================================
        
        VERSIÓN DEFINITIVA: Solución al problema de líneas vacías
        
        Cambios críticos en esta versión:
        - Método alternativo de agrupación por pedido más robusto
        - Uso estratégico de t-field vs t-esc
        - Validaciones exhaustivas en cada paso
        - Fallbacks para asegurar que siempre se muestren datos
        
        ============================================================================
    -->
    
    <template id="report_factura_personalizada">
        <t t-call="web.html_container">
            <t t-foreach="docs" t-as="o">
                <t t-call="plantilla_factura.report_factura_documento" t-lang="o.partner_id.lang"/>
            </t>
        </t>
    </template>
    
    <template id="report_factura_documento">
        <t t-call="web.external_layout">
            <t t-set="o" t-value="o.with_context(lang=lang)" />
            <t t-set="currency" t-value="o.currency_id" />
            
            <div class="page invoice-page">
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 1: CABECERA DE LA FACTURA -->
                <!-- =========================================================== -->
                <div class="row invoice-header mb-4">
                    <div class="col-7">
                        <h2 class="invoice-title">
                            <span t-if="o.move_type == 'out_invoice'">Factura</span>
                            <span t-elif="o.move_type == 'out_refund'">Nota de Crédito</span>
                            <span t-elif="o.move_type == 'in_invoice'">Factura de Proveedor</span>
                            <span t-elif="o.move_type == 'in_refund'">Nota de Crédito de Proveedor</span>
                            <span t-field="o.name"/>
                        </h2>
                        
                        <div class="invoice-date">
                            <strong>Fecha:</strong> <span t-field="o.invoice_date"/>
                        </div>
                    </div>
                    
                    <div class="col-5" t-if="o.referencia_cliente">
                        <div class="client-ref-box border p-3 rounded bg-light">
                            <div class="ref-label text-muted small">Referencia Cliente</div>
                            <div class="ref-value h5" t-field="o.referencia_cliente"/>
                        </div>
                    </div>
                </div>
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 2: INFORMACIÓN DE CLIENTE Y FACTURA -->
                <!-- =========================================================== -->
                <div class="row mb-4">
                    <div class="col-6">
                        <div class="invoice-box border p-3 rounded">
                            <strong class="d-block mb-2">Cliente:</strong>
                            <address t-field="o.partner_id" 
                                     t-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": True}'/>
                            
                            <div t-if="o.partner_id.vat" class="mt-1">
                                <strong>NIF:</strong> <span t-field="o.partner_id.vat"/>
                            </div>
                            
                            <div t-if="o.contacto_facturacion" class="mt-2 border-top pt-2">
                                <strong>Contacto:</strong> <span t-field="o.contacto_facturacion.name"/>
                                <div t-if="o.contacto_facturacion.email" class="small text-muted">
                                    <span t-field="o.contacto_facturacion.email"/>
                                </div>
                                <div t-if="o.contacto_facturacion.phone" class="small text-muted">
                                    <span t-field="o.contacto_facturacion.phone"/>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-6">
                        <div class="invoice-box border p-3 rounded">
                            <table class="table table-sm table-borderless mb-0">
                                <tbody>
                                    <tr>
                                        <td class="field-label"><strong>Fecha Factura:</strong></td>
                                        <td class="field-value"><span t-field="o.invoice_date"/></td>
                                    </tr>
                                    
                                    <tr t-if="o.invoice_date_due">
                                        <td class="field-label"><strong>Fecha Vencimiento:</strong></td>
                                        <td class="field-value"><span t-field="o.invoice_date_due"/></td>
                                    </tr>
                                    
                                    <tr t-if="o.invoice_payment_term_id">
                                        <td class="field-label"><strong>Forma de pago:</strong></td>
                                        <td class="field-value"><span t-field="o.invoice_payment_term_id.name"/></td>
                                    </tr>
                                    
                                    <tr t-if="o.pedidos_relacionados">
                                        <td class="field-label"><strong>Pedidos:</strong></td>
                                        <td class="field-value"><span t-field="o.pedidos_relacionados"/></td>
                                    </tr>
                                    
                                    <tr t-if="o.payment_reference">
                                        <td class="field-label"><strong>Ref. Pago:</strong></td>
                                        <td class="field-value"><span t-field="o.payment_reference"/></td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 3: INSTRUCCIONES ESPECIALES -->
                <!-- =========================================================== -->
                <div t-if="o.instrucciones_especiales" class="row mb-3">
                    <div class="col-12">
                        <div class="alert alert-info">
                            <strong>Instrucciones Especiales:</strong><br/>
                            <span t-field="o.instrucciones_especiales"/>
                        </div>
                    </div>
                </div>
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 4: LÍNEAS DE FACTURA - VERSIÓN DEFINITIVA -->
                <!-- =========================================================== -->
                <!--
                    ESTRATEGIA REVISADA PARA SOLUCIONAR EL PROBLEMA:
                    
                    En lugar de usar lambda complejas y filtered, vamos a:
                    1. Usar el método Python _get_invoice_lines_by_order() si existe
                    2. Iterar de forma más simple y directa
                    3. Asegurar que cada campo se renderiza correctamente
                    4. Añadir validaciones en cada paso
                -->
                
                <!-- Primero, intentar obtener líneas agrupadas por pedido -->
                <t t-set="sale_orders" t-value="o.invoice_line_ids.mapped('sale_line_ids.order_id')"/>
                
                <!-- CASO 1: Hay pedidos relacionados - Mostrar agrupado -->
                <t t-if="sale_orders">
                    <t t-foreach="sale_orders" t-as="order">
                        <!-- Cabecera del pedido -->
                        <div class="order-container mb-4" style="page-break-inside: avoid;">
                            <div class="order-header bg-secondary text-white p-2 rounded-top">
                                <strong>Pedido: <span t-field="order.name"/></strong>
                                <t t-if="order.client_order_ref">
                                    <span class="ms-2">| Ref. Cliente: <span t-field="order.client_order_ref"/></span>
                                </t>
                            </div>
                            
                            <!-- Tabla de líneas -->
                            <table class="table table-sm table-bordered mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th style="width: 40%;">Descripción</th>
                                        <th class="text-end" style="width: 12%;">Cantidad</th>
                                        <th class="text-end" style="width: 13%;">Precio Unit.</th>
                                        <th class="text-end" style="width: 15%;">Impuestos</th>
                                        <th class="text-end" style="width: 20%;">Subtotal</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <!--
                                        CAMBIO CRÍTICO: Iteramos sobre TODAS las líneas
                                        y luego filtramos dentro del bucle con un t-if
                                        
                                        Esto es más robusto porque:
                                        1. QWeb maneja mejor la iteración simple
                                        2. El filtrado es más explícito y debuggeable
                                        3. Podemos añadir validaciones intermedias
                                    -->
                                    <t t-foreach="o.invoice_line_ids" t-as="line">
                                        <!--
                                            Verificar si esta línea pertenece a este pedido
                                            Usamos un enfoque defensivo con verificaciones múltiples
                                        -->
                                        <t t-set="line_orders" t-value="line.sale_line_ids.mapped('order_id')"/>
                                        <t t-set="belongs_to_order" t-value="order.id in line_orders.ids"/>
                                        
                                        <!-- Solo procesar si pertenece a este pedido -->
                                        <t t-if="belongs_to_order">
                                            <!-- Líneas normales (productos) -->
                                            <t t-if="not line.display_type">
                                                <tr>
                                                    <!-- Columna 1: Descripción -->
                                                    <td>
                                                        <strong><span t-field="line.product_id.name"/></strong>
                                                        <div t-if="line.name and line.name != line.product_id.name" 
                                                             class="small text-muted">
                                                            <span t-field="line.name"/>
                                                        </div>
                                                    </td>
                                                    
                                                    <!-- Columna 2: Cantidad -->
                                                    <td class="text-end">
                                                        <!--
                                                            CAMBIO IMPORTANTE: Usamos t-field con widget float
                                                            en lugar de t-esc con formato Python
                                                            
                                                            t-field es más robusto porque:
                                                            - Odoo maneja el formato automáticamente
                                                            - Respeta la configuración regional
                                                            - Maneja correctamente valores None o vacíos
                                                        -->
                                                        <span t-field="line.quantity" 
                                                              t-options='{"widget": "float", "precision": 2}'/>
                                                        <!-- Unidad de medida -->
                                                        <t t-if="line.product_uom_id">
                                                            <span class="ms-1" t-field="line.product_uom_id.name"/>
                                                        </t>
                                                    </td>
                                                    
                                                    <!-- Columna 3: Precio unitario -->
                                                    <td class="text-end">
                                                        <!--
                                                            Usar t-field con widget monetary
                                                            Esto asegura formato correcto con símbolo de moneda
                                                        -->
                                                        <span t-field="line.price_unit" 
                                                              t-options='{"widget": "monetary", "display_currency": currency}'/>
                                                    </td>
                                                    
                                                    <!-- Columna 4: Impuestos -->
                                                    <td class="text-end">
                                                        <t t-if="line.tax_ids">
                                                            <t t-foreach="line.tax_ids" t-as="tax">
                                                                <span t-field="tax.name"/>
                                                                <t t-if="not tax_last">, </t>
                                                            </t>
                                                        </t>
                                                        <span t-else="">—</span>
                                                    </td>
                                                    
                                                    <!-- Columna 5: Subtotal -->
                                                    <td class="text-end">
                                                        <strong>
                                                            <span t-field="line.price_subtotal" 
                                                                  t-options='{"widget": "monetary", "display_currency": currency}'/>
                                                        </strong>
                                                    </td>
                                                </tr>
                                            </t>
                                            
                                            <!-- Líneas de sección o nota -->
                                            <t t-if="line.display_type">
                                                <tr>
                                                    <td colspan="5" 
                                                        t-att-class="'fw-bold' if line.display_type == 'line_section' else 'fst-italic'">
                                                        <span t-field="line.name"/>
                                                    </td>
                                                </tr>
                                            </t>
                                        </t>
                                    </t>
                                </tbody>
                            </table>
                        </div>
                    </t>
                </t>
                
                <!-- CASO 2: No hay pedidos - Mostrar todas las líneas directamente -->
                <t t-else="">
                    <table class="table table-sm table-bordered mb-4">
                        <thead class="table-light">
                            <tr>
                                <th style="width: 40%;">Descripción</th>
                                <th class="text-end" style="width: 12%;">Cantidad</th>
                                <th class="text-end" style="width: 13%;">Precio Unit.</th>
                                <th class="text-end" style="width: 15%;">Impuestos</th>
                                <th class="text-end" style="width: 20%;">Subtotal</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="o.invoice_line_ids" t-as="line">
                                <t t-if="not line.display_type">
                                    <tr>
                                        <td>
                                            <strong><span t-field="line.product_id.name"/></strong>
                                            <div t-if="line.name and line.name != line.product_id.name" 
                                                 class="small text-muted">
                                                <span t-field="line.name"/>
                                            </div>
                                        </td>
                                        <td class="text-end">
                                            <span t-field="line.quantity" 
                                                  t-options='{"widget": "float", "precision": 2}'/>
                                            <t t-if="line.product_uom_id">
                                                <span class="ms-1" t-field="line.product_uom_id.name"/>
                                            </t>
                                        </td>
                                        <td class="text-end">
                                            <span t-field="line.price_unit" 
                                                  t-options='{"widget": "monetary", "display_currency": currency}'/>
                                        </td>
                                        <td class="text-end">
                                            <t t-if="line.tax_ids">
                                                <t t-foreach="line.tax_ids" t-as="tax">
                                                    <span t-field="tax.name"/>
                                                    <t t-if="not tax_last">, </t>
                                                </t>
                                            </t>
                                            <span t-else="">—</span>
                                        </td>
                                        <td class="text-end">
                                            <strong>
                                                <span t-field="line.price_subtotal" 
                                                      t-options='{"widget": "monetary", "display_currency": currency}'/>
                                            </strong>
                                        </td>
                                    </tr>
                                </t>
                                <t t-if="line.display_type">
                                    <tr>
                                        <td colspan="5" 
                                            t-att-class="'fw-bold' if line.display_type == 'line_section' else 'fst-italic'">
                                            <span t-field="line.name"/>
                                        </td>
                                    </tr>
                                </t>
                            </t>
                        </tbody>
                    </table>
                </t>
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 5: TOTALES FISCALES COMPLETOS -->
                <!-- =========================================================== -->
                <div class="row mt-4">
                    <div class="col-7">
                        <div t-if="o.partner_bank_id" class="border p-3 rounded bg-light">
                            <strong class="d-block mb-2">Información Bancaria:</strong>
                            <div><strong>Banco:</strong> <span t-field="o.partner_bank_id.bank_id.name"/></div>
                            <div><strong>IBAN:</strong> <span t-field="o.partner_bank_id.acc_number"/></div>
                            <div t-if="o.partner_bank_id.bank_id.bic">
                                <strong>BIC/SWIFT:</strong> <span t-field="o.partner_bank_id.bank_id.bic"/>
                            </div>
                        </div>
                    </div>
                    
                    <div class="col-5">
                        <table class="table table-sm">
                            <tbody>
                                <tr>
                                    <td><strong>Base Imponible:</strong></td>
                                    <td class="text-end">
                                        <span t-field="o.amount_untaxed" 
                                              t-options='{"widget": "monetary", "display_currency": currency}'/>
                                    </td>
                                </tr>
                                
                                <!-- Desglose de impuestos -->
                                <t t-if="o.tax_totals and o.tax_totals.get('groups_by_subtotal')">
                                    <t t-foreach="o.tax_totals['groups_by_subtotal'].values()" t-as="group_list">
                                        <t t-foreach="group_list" t-as="tax_group">
                                            <tr class="border-top">
                                                <td>
                                                    <span t-esc="tax_group['tax_group_name']"/>
                                                    <div class="small text-muted">
                                                        (Base: <span t-esc="tax_group['formatted_tax_group_base_amount']"/>)
                                                    </div>
                                                </td>
                                                <td class="text-end">
                                                    <span t-esc="tax_group['formatted_tax_group_amount']"/>
                                                </td>
                                            </tr>
                                        </t>
                                    </t>
                                </t>
                                
                                <t t-elif="o.amount_tax">
                                    <tr class="border-top">
                                        <td><strong>Impuestos:</strong></td>
                                        <td class="text-end">
                                            <span t-field="o.amount_tax" 
                                                  t-options='{"widget": "monetary", "display_currency": currency}'/>
                                        </td>
                                    </tr>
                                </t>
                                
                                <tr class="border-top border-dark" style="background-color: #f8f9fa;">
                                    <td class="text-uppercase"><strong>TOTAL:</strong></td>
                                    <td class="text-end">
                                        <strong style="font-size: 1.1em;">
                                            <span t-field="o.amount_total" 
                                                  t-options='{"widget": "monetary", "display_currency": currency}'/>
                                        </strong>
                                    </td>
                                </tr>
                                
                                <t t-if="o.payment_state in ['partial', 'not_paid'] and o.amount_residual > 0">
                                    <tr class="border-top" style="background-color: #fff3cd;">
                                        <td><strong>Pendiente de Pago:</strong></td>
                                        <td class="text-end">
                                            <strong style="color: #856404;">
                                                <span t-field="o.amount_residual" 
                                                      t-options='{"widget": "monetary", "display_currency": currency}'/>
                                            </strong>
                                        </td>
                                    </tr>
                                </t>
                            </tbody>
                        </table>
                    </div>
                </div>
                
                <!-- =========================================================== -->
                <!-- SECCIÓN 6: PIE DE PÁGINA -->
                <!-- =========================================================== -->
                <div t-if="o.narration" class="row mt-4">
                    <div class="col-12">
                        <div class="border-top pt-3">
                            <strong>Observaciones:</strong><br/>
                            <span t-field="o.narration"/>
                        </div>
                    </div>
                </div>
                
                <div class="row mt-3">
                    <div class="col-12">
                        <div class="small text-muted text-center border-top pt-2">
                            <p class="mb-0">
                                Este documento constituye una factura a efectos fiscales. 
                                Conserve este documento según requiere la legislación vigente.
                            </p>
                        </div>
                    </div>
                </div>
                
            </div>
        </t>
    </template>
    
</odoo>
