<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_sale_order_multiple_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.sale.order.multiple.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[hasclass('oe_button_box')]" position="inside">
                <field name="is_consolidated_invoice" invisible="1"/>
                <button name="action_view_source_orders"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-shopping-cart"
                        invisible="not is_consolidated_invoice">
                    <field name="sale_order_ids" widget="statinfo" string="Pedidos origen"/>
                </button>
            </xpath>
            
            <xpath expr="//field[@name='invoice_line_ids']/tree/field[@name='product_id']" position="after">
                <field name="sale_order_id" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
