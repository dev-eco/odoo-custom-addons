<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Acción para vista de lista -->
    <record id="action_account_invoice_download_selected" model="ir.actions.server">
        <field name="name">Descargar PDFs Individuales</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">action = records.action_prepare_individual_downloads()</field>
    </record>
    
    <!-- Añadir botón en la vista de formulario -->
    <record id="view_move_form_with_download_button" model="ir.ui.view">
        <field name="name">account.move.form.download.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header//button[@name='action_register_payment']" position="after">
                <button name="action_prepare_individual_downloads" 
                        string="Descargar PDF" 
                        type="object"
                        invisible="state != 'posted'"
                        groups="account.group_account_user"/>
            </xpath>
        </field>
    </record>
</odoo>
