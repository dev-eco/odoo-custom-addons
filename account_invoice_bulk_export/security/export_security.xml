<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Regla de registro: Historial de exportaciones por compañía -->
        <record id="export_history_comp_rule" model="ir.rule">
            <field name="name">Historial Exportaciones: Multi-Compañía</field>
            <field name="model_id" ref="model_account_invoice_export_history"/>
            <field name="domain_force">[
                '|',
                    ('company_id', '=', False),
                    ('company_id', 'in', company_ids)
            ]</field>
            <field name="groups" eval="[(4, ref('base.group_multi_company'))]"/>
        </record>

    </data>
</odoo>
