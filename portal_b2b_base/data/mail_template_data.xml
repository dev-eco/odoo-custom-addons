<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="email_template_return_notification" model="mail.template">
            <field name="name">Portal B2B - Notificación Devolución</field>
            <field name="model_id" ref="model_sale_return"/>
            <field name="subject">Nueva Solicitud de Devolución - ${object.partner_id.name}</field>
            <field name="email_from">${object.company_id.email or 'noreply@example.com'}</field>
            <field name="auto_delete" eval="False"/>
            <field name="body_html" type="html">
<div style="font-family: Arial, sans-serif;">
    <h2>Nueva Solicitud de Devolución</h2>
    <p><strong>Distribuidor:</strong> ${object.partner_id.name}</p>
    <p><strong>Email:</strong> ${object.partner_id.email or 'No especificado'}</p>
    <p><strong>Teléfono:</strong> ${object.partner_id.phone or 'No especificado'}</p>
    <p><strong>Pedido Original:</strong> ${object.order_id.name if object.order_id else 'No especificado'}</p>
    <p><strong>Motivo:</strong> ${dict(object._fields['reason'].selection).get(object.reason)}</p>
    <h3>Productos a Devolver</h3>
    <table border="1" cellpadding="5" cellspacing="0" style="border-collapse: collapse;">
        <tr>
            <th>Producto</th>
            <th>Cantidad</th>
            <th>Notas</th>
        </tr>
        % for line in object.line_ids:
        <tr>
            <td>${line.product_id.name}</td>
            <td>${line.quantity}</td>
            <td>${line.notes or '-'}</td>
        </tr>
        % endfor
    </table>
    % if object.customer_notes:
    <p><strong>Notas del Distribuidor:</strong></p>
    <p>${object.customer_notes}</p>
    % endif
    <p>Esta solicitud requiere revisión y aprobación en el sistema.</p>
</div>
            </field>
        </record>
    </data>
</odoo>
