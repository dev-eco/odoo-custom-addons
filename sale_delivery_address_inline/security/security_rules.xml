<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Grupos de seguridad -->
    <record id="group_delivery_manager" model="res.groups">
        <field name="name">Gestor de Direcciones de Entrega</field>
        <field name="category_id" ref="base.module_category_sales_sales"/>
        <field name="implied_ids" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

    <!-- Regla multi-empresa para res.partner -->
    <record id="res_partner_delivery_multicompany_rule" model="ir.rule">
        <field name="name">Partner Delivery Addresses: Multi-Company</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="domain_force">[
            '|',
                ('company_id', '=', False),
                ('company_id', 'in', company_ids)
        ]</field>
        <field name="global" eval="True"/>
    </record>

    <!-- Regla multi-empresa para sale.order -->
    <record id="sale_order_delivery_multicompany_rule" model="ir.rule">
        <field name="name">Sale Order: Multi-Company</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="domain_force">[
            ('company_id', 'in', company_ids)
        ]</field>
        <field name="global" eval="True"/>
    </record>

</odoo>
